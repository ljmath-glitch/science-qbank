# 茲茲文教自然科題庫系統 · 完整功能與使用說明

> 這份文件寫給**使用者**（老師、工讀生、主管）：看完就知道系統有哪些分頁、每個功能怎麼用。
> 技術／維護說明另見 `CLAUDE.md`；互動版操作說明書在系統內的「系統文件」（`guide.html`）。
>
> **正式網址**：<https://science-qbank.vercel.app>
> 最後更新：2026-08

---

## 0. 系統是什麼

一套「茲茲文教」內部使用的**國中自然科題庫管理系統**。核心用途：

1. 把段考卷／會考題用 AI 轉成結構化資料，匯入題庫
2. 依領域、單元、知識點、難度、題型分類，可篩選、搜尋
3. 從題庫挑題組卷，匯出成 Word／HTML 考卷
4. 用 AI（Claude／Gemini／ChatGPT／本機 Ollama）自動生成詳解
5. 資料全部存雲端（Supabase），多人同時使用不互相覆蓋

**登入**：開網站後輸入內部密碼（向主管索取）。同一分頁登入後一段時間內不用重複輸入；從別頁的「考卷超連結」點進來會自動免密碼直接跳到對應篩選。

---

## 1. 分頁地圖

| 分頁 | 網址 | 一句話 |
|---|---|---|
| **題庫主系統** | `/index.html` | 篩選、搜尋、新增／編輯、匯入、AI 詳解、組卷匯出的主戰場 |
| **考卷前處理** | `/raw.html` | 把一份新考卷從 PDF/Word 洗成「可匯入的題目＋圖」的加工線 |
| **題庫健檢儀表板** | `/analytics.html` | 一眼看題庫全貌與缺口：題型、難度、圖表、題源、逐年編排變化 |
| **共編進度表** | `/progress.html` | 每份考卷處理到哪一步的看板，多人協作打勾回報 |
| **每日一題** | `/daily.html` | 學生／同仁練習用的每日挑戰，有等級與角色養成 |
| **使用說明書** | `/guide.html` | 系統內建的互動版操作手冊（含工讀生 SOP） |

主系統右上角有分頁切換連結，可直接跳到上面各頁。

---

## 2. 題庫主系統（index.html）

### 2.1 篩選與搜尋
畫面上方是一整排篩選器，可任意組合：

- **文字搜尋**：搜題目內容、摘要、知識點等
- **下拉篩選**：冊數 / 領域（生物・理化・地科）/ 大單元 / 子單元 / 知識點 / 題型 / 難度 / 年度 / 學期 / 學校 / 考試類型 / 次別 / 年級 / 圖表類型 / 待確認

同題組的小題會自動一起展開顯示。每頁 50 題分頁。右上角「↺ 重置」清掉所有篩選，「⋮ 排列」調整排序。

### 2.2 新增／編輯單題
點題目卡片進入編輯視窗，或用「＋ 新增」。可設定：

- 基本識別：年度、學校、**考試名稱（自動組合）**、題號、答案
- 課程對應：年級、冊數、大單元、子單元、知識點、題型、圖表類型
- 難度三分項：步驟分・記憶分・概念分（系統自動換算難度，見 §6）
- 題目內容、詳解，支援 **LaTeX 數學公式**（用 `$...$` 包住，即時預覽）
- **題組 ID**：同一大題的小題填相同代碼，會綁成一組並共用「題組文章」

**考試名稱會依你填的欄位自動組合**；也認得會考／學測／分科測驗／模擬考（見 §7）。

#### 圖片工具
每張題目圖／題組圖下方都有：小・中・大・滿版、寬度滑桿、對齊、套用到全部、移除，以及：

- **🧽 橡皮擦**：把圖裡不要的東西塗白（當白紙背景）或挖空成透明。可調筆刷、復原／重做，**游標圓圈就是筆刷實際大小**，不會誤塗到旁邊。

#### 📄 考卷原稿對照
「考試名稱」欄下方有一條**原稿列**：

- 這份考卷還沒存過原稿 → 按「📄 上傳考卷原稿 PDF」。
- 已存 → 「📄 看原稿」（開新分頁對照）、「換一份」、「移除對應」。

一份 PDF 對應一個考試名稱，**同份考卷所有題目共用**。用途：編輯某題時點「看原稿」開出原卷，一題一題對照有沒有打錯，改下一題再點一次。上傳一次之後不用再重找。

### 2.3 匯入題目（CSV / Excel）
「匯入」按鈕貼上 CSV（或選 Excel 檔）。正式格式 **23 欄**（年度、學校、考試名稱、題號、答案、冊數、冊名、領域、大單元、子單元、知識點、摘要、題型、步驟分、記憶分、概念分、圖表類型、判斷依據、待確認、題目、詳解、題組文章、題組ID）。

- 沒有答案（第 4 欄空白）的題會自動打上「待確認」。
- 偵測到相同考試名稱或相同考試＋題號已存在，會跳確認視窗。
- 「轉檔指令」按鈕可複製一段預寫好的 prompt，貼到任何 AI 對話、附上考卷照片，AI 就會回傳符合格式的 CSV。

### 2.4 AI 生成詳解
詳解欄旁可選四家供應商：

| 供應商 | 說明 |
|---|---|
| **Claude** | 預設，需 API Key |
| **Gemini** | 有模型下拉選單 |
| **ChatGPT (OpenAI)** | 會自動處理模型改名／棄用 |
| **Ollama（本機）** | 免費、資料不出網，需自備電腦跑（見 `CLAUDE.md` 第 7 節） |

- **✨ AI 生成詳解**：單題生成。
- **批次生成詳解**：對目前篩選出的題一次全跑。
- 詳解格式固定用【解題關鍵】【詳解】【觀念補充】；原本沒答案時，AI 會先判斷答案並自動回填。
- API Key 存雲端共用，同事之間用同一顆 Key 不用各自輸入。

### 2.5 挑題與組卷
- **勾選題目**：點卡片核取；同題組小題會一起勾。
- **⚖ 依比例自動選**：在目前篩選條件下，設定簡單／中等／困難／極難各佔幾 %、總題數，隨機抽題。

### 2.6 匯出考卷
勾好題後：

- **📄 預覽考卷**：自訂表頭（年級、科目、範圍、考試名稱）、logo。答案模式可選：**無答案 / 答案卷 / 兩者都要**（分開下載）。
- **📝 下載 Word**（.docx）或 **⬇ 下載 HTML**。
- **選題CSV**：把目前勾的題匯出成 CSV（比匯入格式多一個算好的「難度」欄）。

### 2.7 批次貼圖
「批次貼圖」按鈕：把一批已裁好的圖，依檔名或題號一次貼回對應題目。

- 支援**拖曳上傳**：直接把圖片檔、ZIP、或整個資料夾拖進來。
- 認得檔名格式（如 `Q3_圖.png`、`第3題_描述.jpg`、`P_文章圖.png`）與 ZIP 裡的 `manifest.json`。
- 圖片會依實際像素**用最小合適比例**顯示，不會被放到超大。
- 從「考卷前處理」頁確認好的圖，也會走這條路自動貼回。

### 2.8 待確認／回報鈴鐺 🔔
右上角鈴鐺會抓出所有被標記「有問題」的題（`flag` 且備註以 `[回報]` 開頭），有數字徽章。點開可直接跳轉、編輯、或標記已解決。

### 2.9 備份／還原
- **備份**：把整個題庫匯出成 JSON 存本機。
- **還原**：逐筆寫回雲端（只新增／覆蓋同 id，不會刪掉雲端既有資料）。

---

## 3. 考卷前處理（raw.html）

把一份**新考卷**從原始檔洗成可匯入的資料。分三區：

### 第 I 區：命名與轉檔指令
1. 丟入考卷檔（PDF / DOC / DOCX），系統自動整理出**考試名稱**。
   - 段考：`學校-年級-學年度-上/下學期-第N次段考-科目`
   - **會考／學測／分科測驗**：只給「年度＋類型」（如 `114會考`），跟題庫命名一致。
   - **模擬考**：認得「第幾次模擬考」。
   - ⚠️ 自動整理出的名稱**可以手動修改**——若之後批次貼圖顯示「找不到考卷」，多半是這欄跟題庫實際名稱不一致，直接改成題庫裡的正確名稱即可（欄位有題庫既有名稱的自動完成）。
2. 「轉檔指令」「詳解製作指令」：複製 prompt 給 AI，AI 回傳 CSV 後可直接送進題庫。

### 第 II 區：題目 / 詳解 CSV 交接
貼上或選檔 CSV，檢查欄位無誤後「匯入題目 / 詳解」直接送進題庫。

### 第 III 區：批次貼圖（裁切圖表）
把考卷裡的圖表／表格裁成一張張，對好題號後批次貼回題庫。

- **自動掃描**：Word 內嵌圖 → 數位 PDF 內嵌圖 → AI 二次檢查。
  > ⚠️ 提醒：對「試卷寶清稿」這種**圖片型 PDF**（整頁被壓成一張圖、文字選不起來），自動抽圖天生抓不全（向量圖表、掃描頁無法可靠辨識）。這種請直接用下面的手動框選。
- **手動框選裁切**：在每頁上拖曳框出要的內容（整張圖／整張表含 ABCD／整組選項圖）。最可靠。
- 每張裁切卡片可：
  - **✂ 手動修正**：重新框這一張
  - **🧽 橡皮擦**：塗掉圖裡不要的部分（同主系統，游標顯示筆刷大小）
  - **🔗題組圖**：把圖標記為**題組共用圖**（見 §7），填任一小題代表題號即可，貼圖時整組共用
  - **略過 / 改題號**
- 全部確認後「🖼 批次貼入題庫」，會回報成功、對不上的題號等結果。

---

## 4. 題庫健檢儀表板（analytics.html）

一眼看題庫全貌與缺口，找出該補哪裡：

- **🧩 題型與能力**、**🖼 圖表類型**、**🎚 難度結構**分布
- **科別／領域分布**、**🏫 題源偏差**（哪些學校題偏多／偏少）
- **章節樹**：折疊式，依課綱單元順序排列，看哪些子單元**完全沒有題目**
- **資料品質診斷**：可點開有問題的題目清單（重複、缺欄位、章節對不上等）
- **📅 年度總覽**：逐年的章節編排變化、科目與冊次安排、題型／圖表／難度結構變化
- 手機／窄版會自動排成填滿寬度的卡片

### 4.1 已選取題目的分析
勾選題目後，這頁也能只針對「你目前挑的那批題」做分析，方便組卷時檢查比例。

---

## 5. 共編進度表（progress.html）

多人協作的考卷處理看板：

- 每一份考卷一列，顯示處理進度與**完成率**。
- 各處理步驟可打勾回報狀態，主管一眼看誰卡在哪。
- 有「**考卷圖片高清化**」欄位（追蹤補高清圖的進度，**不計入完成率**）。
- 每份考卷有**超連結**：點了會直接跳到主系統並自動篩出該考卷（免再登入）。

---

## 6. 每日一題（daily.html）

練習用的每日挑戰：

- 每天一題，答對累積、記錄**連續答對**。
- **等級與角色養成**：達到門檻會晉升，可解鎖角色（含隱藏角色愛因斯坦）；有「等級光譜」依門檻比例排列。
- 角色卡可互動：進度環、點頭像講台詞、點光譜看解鎖條件。
- 答錯或有疑問可**回報**（含「答案錯誤／有疑問」選項），回報會進到主系統的待確認鈴鐺。

---

## 7. 幾個要懂的觀念

### 難度怎麼算
步驟分＋記憶分＋概念分加總（三項有一項是 0 視為未評分）：

| 總分 | 難度 |
|---|---|
| 1–4 | 簡單 |
| 5–6 | 中等 |
| 7–8 | 困難 |
| 9+ | 極難 |

### 題型分類（17 種、6 大群）
A 觀念型（A1–A3）/ B 圖表型（B1–B3）/ C 實驗型（C1–C3）/ D 計算型（D1–D3）/ E 素養情境型（E1–E3）/ 非選擇（填、計）。

### 冊數與年級
B1・B2＝七年級，B3・B4＝八年級，B5・B6＝九年級。

### 題組與「題組共用圖」
一群共用同一段閱讀材料的小題叫**題組**，前面那段共用文字＋圖叫**題組文章**。

- **單題圖**：只有一題在用 → 貼到那一題。
- **題組共用圖**：整組小題一起看的圖 → 用「🔗題組圖」標記，填任一小題題號，系統會貼到題組文章，整組共用。

### 考試類型與名稱比對
系統認得**段考／會考／學測／分科／模擬考**五種類型，比對考卷名稱時會把類型納入，避免同年度的會考跟段考張冠李戴。大考（會考／學測／分科）以「年度＋類型」為唯一識別（`114會考自然` 也能對上 `114會考`）。

---

## 8. 常見情境速查

| 我想… | 去哪 | 怎麼做 |
|---|---|---|
| 找某單元的題 | 主系統 | 上方下拉選單篩領域／單元／知識點 |
| 新增一題 | 主系統 | 「＋ 新增」填欄位、貼圖、寫 LaTeX |
| 匯入一整份新考卷 | 考卷前處理 | 第 I 區命名 → 用 AI 轉 CSV → 第 II 區匯入 → 第 III 區補圖 |
| 幫題目生成詳解 | 主系統 | 選 AI 供應商 →「✨ AI 生成詳解」或批次 |
| 出一份考卷 | 主系統 | 勾題（或依比例自動選）→「📄 預覽考卷」→ 下載 Word/HTML |
| 對照原卷檢查 | 主系統 | 編輯題目 →「📄 看原稿」（先上傳過該考卷 PDF）|
| 塗掉圖裡多餘的東西 | 主系統／前處理 | 圖片下方「🧽 橡皮擦」 |
| 看題庫缺哪些題 | 健檢儀表板 | 章節樹看「沒有題目」的子單元 |
| 看誰的考卷卡住 | 共編進度表 | 看完成率與各步驟打勾 |
| 有人回報題目有問題 | 主系統 | 右上角 🔔 鈴鐺 |

---

## 9. 已知限制

- **試卷寶清稿／掃描的圖片型 PDF 無法可靠自動抽圖**——這種請用手動框選，或用外部工具（如 MinerU）先抽圖再批次貼。
- iPhone/iPad 無法使用 Ollama 本機 AI（Safari 混合內容政策無解）。
- `OLLAMA_PRESETS` 的主機 IP 是寫死的，換電腦或重配 Tailscale 要回程式碼更新。
- 只有一個正式 Supabase 環境，沒有測試環境，改資料庫欄位要小心。
